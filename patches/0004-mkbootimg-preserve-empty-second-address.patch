diff --git a/mkbootimg.py b/mkbootimg.py
index e0b0839..5ed3001 100755
--- a/mkbootimg.py
+++ b/mkbootimg.py
@@ -192,7 +192,7 @@ def write_header(args):
     ramdisk_load_address = ((args.base + args.ramdisk_offset)
                             if filesize(args.ramdisk) > 0 else 0)
     second_load_address = ((args.base + args.second_offset)
-                           if filesize(args.second) > 0 else 0)
+                           if filesize(args.second) > 0 or args.set_empty_second_addr else 0)
 
     args.output.write(pack(f'{BOOT_MAGIC_SIZE}s', BOOT_MAGIC.encode()))
     # kernel size in bytes
@@ -515,6 +515,8 @@ def parse_cmdline():
                         help='ramdisk offset')
     parser.add_argument('--second_offset', type=parse_int, default=0x00f00000,
                         help='second bootloader offset')
+    parser.add_argument('--set_empty_second_addr', action='store_true',
+                        help='set the second bootloader load address even when no second bootloader is present')
     parser.add_argument('--dtb_offset', type=parse_int, default=0x01f00000,
                         help='dtb offset')
 
